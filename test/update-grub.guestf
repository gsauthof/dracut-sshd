
# cf. update-grub
 
run
mount /dev/sda3 /
find / | grep loader/entries/ | grep -v rescue | tee entry-name
<! echo download /$(cat entry-name) entry
!sed -i 's/^\(options .\+$\)/\1 rd.luks.uuid='$(cat luks-uuid)'/' entry
<! echo upload entry /$(cat entry-name)
umount /

